import {createSectionInput} from './section-input.js';

document.addEventListener('DOMContentLoaded', () => {
    const sectionContainer = document.querySelector('.cmp-main-container');

   
    createSectionInput(sectionContainer);
});